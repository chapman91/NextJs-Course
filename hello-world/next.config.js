/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
}

module.exports = nextConfig

// 'reactStrictMode' Development mode is a feature for highlighting problems in an application
// Identify and save lifecycles, legacy API usage, and a number of other features 